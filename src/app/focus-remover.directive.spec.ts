import { FocusRemoverDirective } from './focus-remover.directive';

describe('FocusRemoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusRemoverDirective();
    expect(directive).toBeTruthy();
  });
});
